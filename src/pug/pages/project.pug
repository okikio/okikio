extends /layouts/layout.pug

//- block pageInfo
  //- -
  //-   let titleCase = (value) => value.toLowerCase().split(' ').map(function(word) {
  //-       return (word.charAt(0).toUpperCase() + word.slice(1));
  //-     }).join(' ');
    //- let page = {
    //-   url: self.project.url.replace(".html", ""),
    //-   title: `${titleCase(self.project.title)} — okikio`,
    //-   description: self.project.description
    //- };

block content
  //- -
    //- let { next, project } = self;
    //- let { title, description, article } = project;
    //- let image = Object.assign({ src: "cloudinary/city", alt: "An image of ... by ... on unsplash." }, project.image);

  p A Project
